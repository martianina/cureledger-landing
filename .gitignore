# dependencies
node_modules/
.pnp.*
.pnp.js

# Next.js
.next/
out/
dist/
.vercel/

# env and secrets
.env
.env.*.local
.env.production
.env.production.local

# logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# TypeScript
*.tsbuildinfo

# testing / coverage
coverage/
.nyc_output/

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Use one package manager (bun.lock is committed; do not commit duplicate lockfiles)
package-lock.json
yarn.lock

# misc
*.pem
.turbo/
.swc/

# Embedded old scaffold / duplicate (this repo is the live app; do not publish nested copy)
cureledger-landing-main/

# Yarn 2+ local cache in project root (if present)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
